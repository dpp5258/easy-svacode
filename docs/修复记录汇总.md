# easySVA 源码修改对比文档（相对 Gitee）

> 记录本机项目相对 Gitee 源码仓库（`andersonwu/easySVA`、`SVA-backend`、`SVA-web`、`SVA-server`、`SVA-mediaServer`）的全部修改，用于多人协作时同步代码与配置。
>
> 环境：Ubuntu 22.04 · 本机 IP：10.21.224.107 · 访问地址：http://10.21.224.107/ · 账号：admin / admin123

---

## 一、修改总览

| 仓库 | 修改情况 |
|---|---|
| SVA-backend | 2 个文件有修改（live-output 接口、上传路径） |
| SVA-web | 1 个文件有修改（vue.config.js 构建配置） |
| SVA-server | 1 个文件有修改（AvPushStream 编码器回退） |
| SVA-server | **睡岗检测增量（2026-09-03）：14 个源文件修改 + 2 个新文件 + CMakeLists，详见第八节** |
| SVA-mediaServer / easySVA | 无修改（与上游一致） |

---

## 二、源码修改明细（SVA-backend / SVA-web / SVA-server）

### 1. DeploymentController.java — 新增 live-output 接口

- **文件**：`SVA-backend/ruoyi-admin/src/main/java/com/ruoyi/web/controller/deployment/DeploymentController.java`
- **位置**：`stop` 方法与 `get` 方法之间
- **差异**：+22 行（git diff 与 Gitee 对比的唯一新增代码）
- **背景**：前端（Gitee 原版）已实现 `POST /deployments/{id}/live-output` 调用（`src/api/deployment.js` + `add.vue:1518`、`index.vue:264`、`center-switch-panel.vue:314`），后端缺对应路由，请求命中 Spring 静态资源兜底报错。本修改补齐该接口，返回算法画框输出流地址。

新增方法完整代码：

```java
    @PostMapping("/{id}/live-output")
    public AjaxResult liveOutput(@PathVariable("id") String id)
    {
        DeploymentTask record = deploymentTaskService.selectDeploymentTaskById(id);
        if (record == null)
        {
            return AjaxResult.error("布控任务不存在");
        }

        String algorithmStreamUrl =
            deploymentAnalyzerClient.buildAlgorithmStreamUrl(record.getDeviceId(), id);
        if (StringUtils.isEmpty(algorithmStreamUrl))
        {
            return AjaxResult.error("未绑定可用ZLM/SVA服务器或配置缺失，无法生成算法输出流地址");
        }

        Map<String, Object> data = new LinkedHashMap<>();
        data.put("algorithmStreamUrl", algorithmStreamUrl);
        data.put("algorithm_stream_url", algorithmStreamUrl);
        return AjaxResult.success(data);
    }
```

说明：

- 复用 `DeploymentAnalyzerClient.buildAlgorithmStreamUrl(apeId, deploymentId)`（该文件第 283 行），与 create/update 生成地址逻辑一致，输出 `ws://{zlmHost}:{zlmMediaHttpPort}/{svaApp}/{deploymentId}.live.flv`；
- 同时返回 `algorithmStreamUrl` 与 `algorithm_stream_url`，兼容前端两种取值；
- 无新增 import（`PostMapping`、`PathVariable`、`AjaxResult`、`StringUtils`、`Map`、`LinkedHashMap` 文件头均已存在）。

### 2. application.yml — 上传路径

- **文件**：`SVA-backend/ruoyi-admin/src/main/resources/application.yml`
- **差异**：1 行

```yaml
ruoyi:
  profile: /var/www/SVA-web/upload     # Gitee 原值：D:/ruoyi/uploadPath
```

### 3. SVA-web/vue.config.js — 转译 @opentiny 依赖

- **文件**：`SVA-web/vue.config.js`
- **差异**：+2 行（`module.exports` 内新增）：
- **背景**：上游依赖升级后（`@opentiny/fluent-editor` 引入 quill 2.x、`@opentiny/utils` 等），webpack 4 无法解析其 class 静态字段/`??` 语法，`npm run build:prod` 直接失败（31 个编译错误）。

```js
  // 让 babel 转译 @opentiny 系列（quill2/@opentiny-utils 语法 webpack4 无法解析）
  transpileDependencies: [/@opentiny/],
```

### 4. SVA-server/AvPushStream.cpp — 编码器打开失败回退

- **文件**：`SVA-server/Analyzer/Core/AvPushStream.cpp`
- **差异**：约 +20 行（编码器选择逻辑改造）
- **背景**：原逻辑只在"找不到 h264_nvenc"时回退软件编码；在无 NVIDIA GPU 的机器上（如 AMD CPU 机器），`h264_nvenc` 存在但 `avcodec_open2` 打开失败（`Cannot load libcuda.so.1`），推流直接失败且无回退。现改为：nvenc 打开失败后自动改用 libx264 软件编码（日志可见 `falling back to software H.264 encoder`）。

---

## 三、部署配置（/opt/SVA 与系统配置，非源码）

### 1. 端口地址汇总

| 组件 | 端口 | 配置 |
|---|---|---|
| MariaDB | 3306 | 库 `easySVA`（42 表，由 Gitee 仓库 `data_20250520.sql` 初始化），root 密码 `easySVA.EZ` |
| Redis | 6379 | 本地 |
| SVA-backend | 9114 | `server.port` |
| ZLMediaKit | 9992 HTTP / 9994 RTSP / 9995 RTMP | secret `V3522025zlm0aA9ajn7UiOWi` |
| SVA-server 分析器 | 9991 admin / 9993 推理 | `/opt/SVA/config.json` |
| Nginx | 80 | 前端页面 + 反向代理 |
| mediamtx（可选测试用） | 8554 | `rtsp://127.0.0.1:8554/test` |

### 2. /opt/SVA/config.json（分析器）

与仓库样例 `SVA-server/config.json` 仅一处不同：

```json
{
  "code": "node-353",
  "name": "name",
  "describe": "describe",
  "host": "10.21.224.107",
  "adminPort": 9991,
  "mediaHttpPort": 9992,
  "analyzerPort": 9993,
  "mediaRtspPort": 9994,
  "mediaSecret": "V3522025zlm0aA9ajn7UiOWi",
  "uploadDir": "/var/www/SVA-web/upload",
  "modelDir": "/opt/SVA/models",
  "saveAlarmType": 1,
  "saveAlarmUrl": "http://127.0.0.1:9114/waring/waring/addFromSvaSimple",
  "detectEventUrl": "ws://127.0.0.1:9114/websocket/sva/noop"
}
```

差异：`host` 由仓库样例 `10.129.52.114` 改为本机 IP `10.21.224.107`。

### 3. Nginx（/etc/nginx/sites-enabled/default）

```nginx
upstream websocket_backend { server 127.0.0.1:9114; }

server {
    listen 80 default_server;
    listen [::]:80 default_server;
    root /var/www/SVA-web/dist/;
    index index.html index.htm index.nginx-debian.html;
    server_name _;

    location / { try_files $uri $uri/ =404; }
    location /alarm/       { alias /var/www/SVA-web/upload/alarm/; }
    location /zlm/         { alias /var/www/SVA-web/upload/storage/; }
    location /prod-api/    { proxy_pass http://127.0.0.1:9114/; proxy_set_header Host $http_host; }
    location /websocket/   {
        proxy_pass http://websocket_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 600s;
    }
}
```

对应前端 `.env.production` 的 `VUE_APP_BASE_API=/prod-api`。

### 4. 进程管理

- 进程由 `/home/jun/桌面/easySVA/start.sh`（nohup 方式）启停，无开机自启；
- MariaDB / Redis / Nginx 为系统服务。


## 四、初始分卷压缩包（Analyzer-lib_zip.part*.rar）解压与安装

> 前置：分卷压缩包已下载到本机 `/home/jun/下载/`（3 个分卷：`Analyzer-lib_zip.part1.rar` 2GB、`part2.rar` 2GB、`part3.rar` 465MB，合计约 4.76GB）。分卷包内是 4.76GB 的 `Analyzer-lib.zip`（即 README 所述 easySVA-lib 依赖包，含 YOLO 模型与全部编译依赖），后续步骤从解压开始。

### 1. 确认分卷完整

```bash
ls -l /home/jun/下载/Analyzer-lib_zip.part*.rar
# 应有 3 个分卷：part1.rar（2GB）、part2.rar（2GB）、part3.rar（465MB）
# 缺一个分卷都无法解压
```

### 2. 解压分卷 RAR（得到 Analyzer-lib.zip）

```bash
cd /home/jun/下载
unrar x Analyzer-lib_zip.part1.rar    # 只需指定 part1，unrar 自动读取后续分卷
# 解出：Analyzer-lib.zip（约 4.76GB）
```

### 3. 放置到 /opt（脚本要求 install_source.sh 与依赖包都在 /opt）

```bash
sudo cp /home/jun/下载/Analyzer-lib.zip /opt/
sudo cp /home/jun/桌面/easySVA/easySVA/install_source.sh /opt/
```

### 4. 解压 zip 并核对目录名

```bash
cd /opt && sudo unzip Analyzer-lib.zip
# 解出 /opt/Analyzer-lib/；脚本写死 cd /opt/easySVA-lib，需改名：
sudo mv /opt/Analyzer-lib /opt/easySVA-lib
ls /opt/easySVA-lib/
```

### 5. 核对包内内容（脚本编译依赖这些文件）

```
models/                                   # yolo11n.onnx / yolo26s.onnx / yolo26s_miner.onnx / yolo26s_miner.yaml
ffmpeg-6.1.4.tar.xz
opencv-4.13.0.zip / opencv_contrib-4.13.0.zip
onnxruntime-linux-x64-1.26.0.tgz                          # CPU 版用
onnxruntime-linux-x64-gpu_cuda13-1.26.0.tgz               # GPU 版用
nv-codec-headers.zip / curl-7.83.0.zip
cuda_13.1.2_590.48.01_linux.run                           # GPU 版用（约 4.3GB）
llama.cpp-minicpm-omni.zip / 编译第三方库.md
```

### 6. 执行安装

```bash
cd /opt && sudo -s        # 脚本要求 root
./install_source.sh       # 过程中选择 GPU(G) / CPU(C) 版本
```

要点：分卷缺一不可；`unrar` 只需指定 part1；解压后先 `ls` 核对目录名（`easySVA-lib`）与内容再跑脚本，避免脚本 `cd /opt/easySVA-lib` 失败。

---

## 五、一键安装脚本（easySVA/install_source.sh）部署修正

> 该脚本为 Gitee `easySVA` 仓库自带的官方安装脚本（本机未改动）。以下仅列出部署时**影响实际效果**的一处修正（不涉及个人后续修改）。

| # | 问题 | 位置（行号） | 修改方式 |
|---|---|---|---|
| 1 | 后端上传目录为 Windows 残留 `D:/ruoyi/uploadPath`，Linux 下告警图片/录像保存到错误路径，经 nginx `/alarm/`、`/zlm/` 别名访问不到 | 373 行与 374 行之间 | 插入 sed 替换 |

### 修改 1：后端上传路径（374 行 mvn 之前插入）

- **位置**：373 行 `cd /opt/SVA/SVA-backend` 与 374 行 `mvn clean package -Dmaven.test.skip=true` 之间
- **原文**：（此处无任何处理）
- **改为**：插入以下内容：

```bash
# 373 行后、374 行前插入：替换 Windows 残留上传路径
sed -i 's|D:/ruoyi/uploadPath|/var/www/SVA-web/upload|' \
  /opt/SVA/SVA-backend/ruoyi-admin/src/main/resources/application.yml
```

---

## 六、构建与部署

```bash
# 构建后端
cd SVA-backend
mvn -Dmaven.repo.local=/home/jun/.m2/repository clean package -Dmaven.test.skip=true -o

# 部署（旧包备份 → 换新包 → 重启）
cp /opt/SVA/backend/backend.jar /opt/SVA/backend/backend.jar.bak.20260901
cp ruoyi-admin/target/ruoyi-admin.jar /opt/SVA/backend/backend.jar
sudo /home/jun/桌面/easySVA/start.sh restart
```

回滚：`sudo cp /opt/SVA/backend/backend.jar.bak.20260901 /opt/SVA/backend/backend.jar` 后 `start.sh restart`。

---

## 七、验证结果（已实测）

| 检查项 | 结果 |
|---|---|
| `POST /deployments/{id}/live-output`（正常 id） | 200，返回 `algorithmStreamUrl`（ws://…/analyzer/{id}.live.flv） |
| `POST /deployments/{id}/live-output`（不存在 id） | 返回"布控任务不存在" |
| `GET /deployments/{id}`（详情） | 200，任务/算法/流地址/画布区域数据完整 |
| `GET /algorithm/config/list` | 3 条算法 |
| `GET /algorithm/config/targets/{code}` | 各算法检测目标正常返回 |
| 算法画框输出流 | ffprobe 实测 h264 1920x1080 + aac（RTSP 与 HTTP-FLV） |

---

## 八、2026-09-03 分析器睡岗检测增量（A1~A8）

> 提交范围:`c20b640`(需求手册)、`99722af`~`440b5f5`(A1~A8 共 8 个功能提交),已推送 `origin/main`。
> 详细接口/参照项目/功能对应见 `docs/分析器睡岗集成-修改说明与接口参照.md`;规格依据 `docs/分析器双线集成实现需求手册.md`。

### 1. 改动清单(全部插入式增量,现有代码行零修改)

| 功能 | 文件 | 要点 |
|---|---|---|
| A1 姿态数据结构 | `SVA-server/Analyzer/Core/TrackMetadata.h`、`Algorithm.h` | 新增 `PoseKeypoint/PosePitchSample`;`DetectObject` 追加 4 个默认值字段 |
| A2 Pose 解码器 | `SVA-server/Analyzer/Core/AlgorithmOnYolo.h/.cpp` | 新解码器 `DensePoseWithNms`(1×56×N 关键点),老解码器零改动 |
| A3 姿态几何 | `SVA-server/Analyzer/Core/Utils/PoseUtils.h/.cpp`(新) | `computePosePitchDeg`(肩→耳/鼻 对竖直轴夹角) |
| A4 时序俯角历史 | `SVA-server/Analyzer/Core/TemporalContext.h/.cpp` | 每 track 关键点 EMA(α=0.6)+ 俯角历史(64条/10s) |
| A5 睡岗规则 | `SVA-server/Analyzer/Core/BehaviorEvaluator.h/.cpp` | `sleep_post`:窗口内俯角≥阈值最长连续段≥thresholdMs |
| A6 规则配置 | `SVA-server/Analyzer/Core/Control.h` | `headPitchThresholdDeg`(默认 60°)+ 默认值 + 白名单外补救 |
| A7 引擎注册 | `SVA-server/Analyzer/Core/Scheduler.h/.cpp`、`Analyzer.cpp` | try/catch 加载 pose 模型(缺模型可启动)+ 析构释放 + code 映射 |
| A8 骨架可视化 | `SVA-server/Analyzer/Core/Worker.cpp` | serverOverlay 骨架/俯角绘制 |
| 构建 | `SVA-server/CMakeLists.txt` | 源列表 + include 目录 |

### 2. 对外契约(新增)

- 算法 code:`on_yolo11n_pose_sleep`(兼容别名 `ov_yolo11n_pose_sleep`);模型 `yolo11n_pose_sleep.onnx` → `/opt/SVA/models`
- 规则:`behaviorRules[].behaviorType = "sleep_post"`(白名单外新类型,分析器侧已兼容);`thresholdMs` 默认 5000ms;`headPitchThresholdDeg` 默认 60°
- 事件/告警:复用现有 detect.event → bind-media → 素材回调链路,携带 `behaviorType=sleep_post`

### 3. 待办依赖(他人分工)

- 角色 2:导出 ONNX 模型与睡岗样本视频
- 角色 4:后端告警类型映射(`sleep_post`→`SVA_SLEEP_POST`,需求单 `docs/后端sleep_post映射需求单.md`)+ `av_algorithm` 登记行
- 角色 5:前端布控"睡岗检测"选项与告警展示

### 4. 验证结果(已实测)

| 检查项 | 结果 |
|---|---|
| CPU 构建(`cmake .. -DSVA_ONNXRUNTIME_GPU=OFF`) | 通过 |
| 重启加载新二进制 | 老 2 模型正常;pose 缺模型仅告警("睡岗模型加载失败(不影响启动)");`Start Success` |
| `/api/health` | code 1000 |
| 老算法回归(`on_yolo26n_80` + 本地测试流) | `add success`;`detectFrameSent` 持续增长;cancel 正常;日志无错误 |
