# 分析器睡岗检测增量 —— 当前修改说明(接口/参照/功能对应)

> 作者:分析器开发(YukinoShelley 小组角色 3)
> 日期:2026-09-03 · 分支:`main`(已推送到 https://github.com/YukinoShelley/easy-svacode )
> 范围:提交 `c20b640`(需求手册)与 `99722af`~`440b5f5`(A1~A8 实现,共 9 个提交)
> 依据:docs/分析器双线集成实现需求手册.md

## 一、本阶段做了什么(一句话)

把 YOLO-Pose 睡岗检测以**纯增量**方式接入 SVA-server:新增关键点解码器 + 俯角几何 + 时序判定规则 `sleep_post`,全部复用现有布控/告警/推流/上报链路;**现有代码行零修改**(仅一处 swapRB 表达式的等价泛化),缺模型可正常启动,老算法零回归。

## 二、功能 ↔ 代码 ↔ 提交对应表

| 功能 | 涉及文件 | 接口/关键函数 | 提交 |
|---|---|---|---|
| 检测结果携带姿态数据 | `Core/TrackMetadata.h`、`Core/Algorithm.h` | 新结构体 `PoseKeypoint`、`PosePitchSample`;`DetectObject` 新增 `keypoints/keypointsPresent/posePitchDeg/posePitchHistory` | `99722af` |
| 姿态几何计算 | `Core/Utils/PoseUtils.h/.cpp`(**新文件**) | `computePosePitchDeg(keypoints, pitchDeg, noseShoulderGap, visibilityAvg)`;索引常量 `PoseIndex`(鼻0/耳3,4/肩5,6) | `5795edc` |
| YOLO-Pose 解码 | `Core/AlgorithmOnYolo.h/.cpp` | 解码器枚举新增 `DensePoseWithNms`;新函数 `decodeDensePoseWithNms`(解析 1×56×N);profile 选择(算法 code 含 `pose_sleep`);runInference 新路由 | `07deb64` |
| 每目标俯角历史(时序) | `Core/TemporalContext.h/.cpp` | `TemporalTrackState` 新增姿态字段;`updatePoseTemporalState()`(关键点 EMA α=0.6 → 采样,64 条/10s 双上限,丢帧断连续段);`updateMatchedTrack/createTrack/writeTemporalFields` 末尾追加 | `f4bc42a` |
| 布控规则可配置俯角阈值 | `Core/Control.h` | `BehaviorRuleConfig.headPitchThresholdDeg`(默认 60°);JSON 解析键 `headPitchThresholdDeg`/`pitchThresholdDeg`;默认值块新增 `sleep_post`(thresholdMs 默认 5000ms);白名单外补救(不改白名单行) | `45b8d5a` |
| 睡岗判定规则 | `Core/BehaviorEvaluator.h/.cpp` | 新函数 `isSleepPostHit`;分派新增 `sleep_post` 分支(窗口内俯角≥阈值的最长连续段 ≥ thresholdMs 即命中;静止约束默认关) | `87b2310` |
| 引擎注册 | `Core/Scheduler.h/.cpp`、`Core/Analyzer.cpp` | 新成员 `on_yolo11n_pose_sleep`;`initAlgorithm()` 尾部 try/catch 加载 `yolo11n_pose_sleep.onnx`(缺模型仅告警);`resolveAlgorithm()` 新映射;析构释放 | `dcb20a7` |
| 骨架可视化 | `Core/Worker.cpp` | serverOverlay 新增骨架连线/关键点/俯角文字绘制块 | `440b5f5` |
| 构建登记 | `CMakeLists.txt` | source 列表加 `PoseUtils.cpp`;target 加 include 目录 | `5795edc` |

## 三、相关接口(对外契约,后端/前端/AI 角色据此对接)

### 1. 布控下发 JSON(经现有 `POST :9993/api/control/add`,无新路由)

```json
{
  "code": "布控编号",
  "streamUrl": "rtsp://...",
  "algorithmTasks": [
    { "algorithmCode": "on_yolo11n_pose_sleep", "objectCodes": ["person"],
      "detectFps": 8, "scoreThreshold": 0.5, "nmsThreshold": 0.5 }
  ],
  "regions": [{ "id": "region_1", "primary": true, "points": [0,0,1,0,1,1,0,1] }],
  "geometryConfig": {
    "behaviorRules": [{
      "id": "sleep_rule_1", "name": "睡岗检测",
      "customEventName": "睡岗", "behaviorType": "sleep_post",
      "enabled": true, "geometryId": "region_1",
      "thresholdMs": 5000, "headPitchThresholdDeg": 60,
      "ruleObjectCode": "person"
    }]
  }
}
```

- `behaviorType="sleep_post"`(白名单外的**新类型**,分析器在规范化后补救识别);
- `headPitchThresholdDeg` 为新增可选规则字段(别名 `pitchThresholdDeg`),缺省 60°;`thresholdMs` 缺省 5000ms(联调建议档位 3000/4000/5000);
- 静止约束可选:`maxSpeedPxPerSec > 0` 时要求目标静止(默认不约束)。

### 2. 事件与告警(完全复用现有链路,分析器侧零新增上报代码)

- 命中后 `DetectFrameEvent` 携带 `behaviorType="sleep_post"`、`customEventName="睡岗"`、`ruleId`、`regionId/regionName`、`trackId`;
- 后续:WS detect.event → 后端建 h_waring → 回拨 `/api/alarm/bind-media` → 分析器生成截图/录像 → 回调 `addFromSvaMediaCallback`(**需后端先补映射,见 docs/后端sleep_post映射需求单.md**)。

### 3. 模型契约(AI 算法角色导出时核对)

| 项 | 约定 |
|---|---|
| 模型文件 | `yolo11n_pose_sleep.onnx`,放置 `/opt/SVA/models`(root),重启分析器生效 |
| 输出形状 | `1×56×N`:cx,cy,w,h + person 类分 + 17×(x,y,conf)(与 ultralytics Pose 头一致) |
| 输入 | 640×640 方形 pad、RGB(BGR→RGB,即 profile `square_rgb`) |
| 类别 | 仅 person(id 0);objectness 阈值 0.5、NMS 0.5(引擎内置,与老 Dense 一致) |
| 验收标志 | 分析器日志出现 `decoder=dense_pose_with_nms` 且无 `睡岗模型加载失败` |

### 4. 算法字典登记(后端角色执行)

```sql
INSERT INTO `av_algorithm` (`sort`,`code`,`name`,`api_url`,`object_count`,`object_str`,`remark`,`state`,`create_time`,`update_time`)
VALUES (3,'on_yolo11n_pose_sleep','睡岗检测(yolo11n-pose)','',1,'person','睡岗检测增量:持续低头超过阈值告警',0,NOW(),NOW());
```

## 四、参照的开源项目(已实读源码核对;实现时逐处对照)

| 参照项目 | 地址 | 对照的代码 | 本实现对应 |
|---|---|---|---|
| Ultralytics YOLO | https://github.com/ultralytics/ultralytics | `ultralytics/nn/modules/head.py` class Pose(kpt_shape=(17,3),nk=51)→ 输出 56 列 | A2 解码布局(框4+类1+17×3) |
| 同上 | 同上 | `ultralytics/utils/ops.py:612 scale_coords`(减 pad→除 gain→clip) | A2 坐标映射(本 fork 方形 pad,等价因子 paddedSize/mInputWidth) |
| 同上 | 同上 | `ultralytics/engine/results.py:1202 class Keypoints`(N×17×3=x,y,conf) | A1 DetectObject.keypoints 设计 |
| 同上 | 同上 | `ultralytics/solutions/solutions.py:464-470 _estimate_pose_angle_cached`(atan2 三点角) | A3 角度原语(本实现改为"肩-头向量对竖直轴",代码注释有推导) |
| 同上 | 同上 | `ultralytics/solutions/ai_gym.py:84-96`(每 track state、阈值→stage→计数) | A4/A5 时序状态机对照 |
| 同上 | 同上 | `ultralytics/utils/plotting.py:555 Annotator.kpts`(骨架连线/置信度过滤) | A8 骨架绘制参数 |
| MediaPipe | https://ai.google.dev/edge/mediapipe/solutions/vision/pose_landmarker | 关键点语义参考 | A3 头/肩关键点选择(仅思路参照,未引入依赖) |
| ZLMediaKit | https://github.com/ZLMediaKit/ZLMediaKit | `src/Rtp/RtpProcess.h:21`(app=rtp)、`server/WebApi.cpp:1467-1502`(openRtpServer port+stream_id) | 国标流命名契约 `rtp/{国标流ID}`(B1,供后端/流媒体角色) |
| WVP-PRO | https://github.com/648540858/wvp-GB28181-pro | `gb28181/transmit/event/request/impl/RegisterRequestProcessor.java` 等 | 国标 SIP 信令流程参照(流媒体角色) |

其他参考:MMPose https://github.com/open-mmlab/mmpose(备选模型)、ByteTrack https://github.com/ifzhang/ByteTrack(backlog)、EasyGBS https://github.com/EasyGBS/EasyGBS、ZLM GB28181 文档 https://docs.zlmediakit.com/zh/guide/protocol/gb28181/。

## 五、验证情况(已测 / 未测)

**已测**:①编译通过(CPU 构建 `-DSVA_ONNXRUNTIME_GPU=OFF`);②重启加载新二进制:老 2 模型正常、pose 缺模型仅告警不崩溃、`Start Success`;③`/api/health` code 1000;④老算法回归:`on_yolo26n_80` 布控(本地 ffmpeg 测试流经 ZLM)add success、`detectFrameSent` 持续增长、cancel 正常、日志无错误。

**未测(依赖外部前置条件)**:①睡岗真实触发/防误报(等 `yolo11n_pose_sleep.onnx` 与样本视频,角色 2);②告警入库闭环(等后端映射,见需求单);③国标流兼容 B4(等流媒体角色 SIP)。

## 六、如何回退本阶段修改

```bash
git revert 440b5f5^..99722af --no-commit   # 或逐个 revert;更简单:git reset --hard 99722af^ 再 push -f
```
注意:回退会连同需求手册提交 `c20b640` 一并回退时请只选 A1~A8 的 8 个提交范围。
