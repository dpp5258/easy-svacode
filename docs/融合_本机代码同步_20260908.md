# 融合：教室机(10.122.207.41)最新代码同步到 easy-svacode（2026-09-08）

> 本文件记录 2026-09-08 从教室机 `/opt/SVA`（权威最新）同步进本库的**新增内容**与**运行/部署配置要求**。
> 同步范围：`SVA-backend`、`SVA-server`、`SVA-web` 三个源码树整体更新为本机版（241 处修改 + 11 个新增文件）；`docs/ models/ records/ changes/ db-backup/` 未动。
> 本机 `/opt/SVA` 未被改动，仍为权威源。

---

## 一、各仓库新增内容清单

### 1. SVA-backend（Java，国标接入 + PTZ + 推流鉴权）
| 文件/类 | 内容 |
|---|---|
| `waring/domain/WvpServer.java` + `WvpServerMapper` + `WvpServerController` | WVP 平台注册表（host/18080、admin 凭据、enabled），多平台支持 |
| `waring/domain/HDevice.java` 扩展 | 新增 `device_type(GB28181/RTSP)`、`gb_id/gb_channel_id/gb_domain/gb_password`、`sip_server`、`wvp_server_id`、`status` |
| `waring/service/impl/WvpClient.java` | WVP REST 客户端：登录 token 按平台缓存、设备列表/通道、play start/stop、设备增删改名、**云台 PTZ/归位/预置位**；**401 自动重登重试**（token 1h 过期自愈） |
| `waring/controller/GbDeviceController.java`（新增） | 国标登记 `/waring/device/gb/register`、一键模拟器 `/sim/start|stop|list` |
| `waring/service/impl/GbSimulatorService.java`（新增） | 托管本机 `gb28181_pusher.py` 启停（GST_PLUGIN_PATH 注入、source 全透传） |
| `waring/startup/`（新增） | 启动初始化 |
| `web/service/deployment/DeploymentAnalyzerClient.java` | Analyzer 回推流 URL 追加 `?sign=md5(pushKey)`（修布控 401） |
| `application.yml` | 新增 `easySva.wvp.pushKey` 配置节 |
| 控制器/服务 | `HDeviceController/HDeviceService(+Impl)`：PTZ 4 接口、GB28181 校验、状态同步 |

### 2. SVA-web（Vue，国标/云台/睡岗 UI）
| 文件 | 内容 |
|---|---|
| `views/device/manage.vue` | 国标登记弹窗（20 位编码 + 视频源预设/自定义 test/file/rtsp/v4l2）、行内**云台控制**按钮 + PTZ 弹窗（8 向长按连发/变焦/归位/预置位）；**操作列加宽 510** 防按钮溢出 |
| `api/device.js` | +`ptzControl/ptzHome/ptzPresetCall/ptzPresetAdd` 等接口 |
| `views/deployment/SleepRuleAdvancedParams.vue`、`sleepAdvancedParams.js`（新增） | 睡岗布控高级参数 |
| 其它 | 国标设备列表/状态、告警适配 |

### 3. SVA-server（C++，睡岗推理线）
- YOLO-Pose 睡岗姿态判定（状态机/时序闸门）、ROI 放大二次推理、推流/告警链路兼容（13 处，融合自算法/分析器线）。

### 4. 数据库变更（easySVA 库）
- `h_device` 扩展：`device_type / gb_id / gb_channel_id / gb_domain / gb_password / sip_server / wvp_server_id / status`
- 新增注册表：`wvp_server`（WVP 平台）、`zlm_server`、`sva_server`

---

## 二、配置要求（换机/部署必读）

### 1. 依赖环境
JDK 17、MySQL/MariaDB、Redis、nginx（前端发布，可选）、python3 + GStreamer（国标模拟器）。

### 2. 数据库（两库 + 服务器注册行）
| 库 | 内容 | 连接（默认） |
|---|---|---|
| `easySVA` | 业务库（含 h_device、告警、布控） | root / easySVA.EZ（可按需改，但后端 druid 配置要同步） |
| `wvp` | WVP-PRO 平台库（wvp_user 含 **push_key**） | 同上 |
- **换机必须改**：`wvp_server` 表 `host`（后端→WVP 地址）、`zlm_server/sva_server` 行 `host`。
- 导出/导入：`db-backup/`（队友）或教室机 `mysqldump`。

### 3. WVP（wvp-pro-2.7.4）`application.yml`
```yaml
sip:
  ip: <本机IP>            # 必改：模拟器/摄像头要能 UDP 5060 到达
  id: 34020000002000000001
  domain: 3402000000
  password: 12345678      # 设备公共认证
spring.data.redis.database: 15
media:                    # 指向 ZLMediaKit
  id: Aodpt9CbTmrRBOMv
  ip: 127.0.0.1
  http-port: 9992
  secret: V3522025zlm0aA9ajn7UiOWi
```

### 4. easySVA 后端（jar 内置配置，外置覆盖放 `config/`）
| 键 | 值/说明 |
|---|---|
| `easySva.wvp.pushKey` | `3e80d1762a324d5b0ff636e0bd16f1e3`（**必须 = wvp 库 `wvp_user.push_key`**，决定推流 sign） |
| datasource | easySVA 库 root/密码 |
| redis | localhost:6379 **db3** |
| `ruoyi.profile` | `/var/www/SVA-web/upload`（告警截图落盘） |

### 5. 国标模拟器（无真机时）
- 脚本：`gb28181_pusher.py`（sbgb28181），启动需 `GST_PLUGIN_PATH=<sim>/gst-gb28181sink/build`。
- **v4l2 源已修复**：USB 摄像头(YUYV) → `videoconvert + x264enc` 编码后再封装（原先直接 `h264parse` 会 not-negotiated）。
- 视频源：`file:///…mp4`(H264)、`test`(彩条)、`rtsp://…`、`v4l2:///dev/video0`。

### 6. 关键契约速查
| 项 | 值 |
|---|---|
| WVP REST 登录 | `GET /api/user/login?username=&password=`（password=md5(admin)） |
| token | JWT **有效期 1h**，后端已做 401 自动重登 |
| 推流 sign | `sign=md5(pushKey)`；Analyzer 回推 URL 已自动拼 |
| PTZ 端点 | 前端 `/waring/device/ptz/{apeId}` → 后端 → WVP `/api/front-end/ptz/...`、home `/api/device/control/home_position?enabled=true` |
| 归位超时 | 模拟器只回 SIP 200、不回业务应答 → `消息超时未回复` 属**模拟器预期**；真机正常 |

---

## 三、验证记录（2026-09-08 实测）
- 3 台国标模拟器（test/wx1 素材/本机 USB 摄像头）REGISTER 上线、WVP 点播全通（`code:0` flv 返回）；
- 后端 401 自动重登验证：token 过期后调用自动恢复；
- WVP 重启恢复实验：注册会话清空 → 模拟器 41s 内自动重 REGISTER 回 ONLINE；
- easySVA 侧（backend/Analyzer/ZLM）重启对 WVP 注册会话零影响。

## 四、边界与待办
- 国标链路用模拟器验证；**真机接入待验收**（注册/预览已就绪；PTZ 需真机业务应答）。
- 睡岗推理（sva-server + models）完整演示需教室机或自行构建。
- `SVA-mediaServer` 本库未收录（官方原版无改动）。
- 课程 gitee 验收动作（fork 老师 4 仓库 + star）与本整合库无关，需另行完成。
